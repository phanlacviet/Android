@model IEnumerable<K22CNT2_PhanLacViet_2210900079_project2.Models.CTDH>

@{
    ViewBag.Title = "Index";
}

<p>
    <a href="@Url.Action("Create")" class="btn btn-success">Thêm mới</a>
</p>
<table class="table table-bordered">
    <tr style="background-color: antiquewhite">
        <th>
            <p>Tên sản phẩm</p>
        </th>
        <th>
            <p>Giá cả</p>
        </th>
        <th>
            <p>Mã khách hàng</p>
        </th>
        <th>
            <p>Tên sản phẩm</p>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenSP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaCa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonHang.MaKH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SanPham.TenSP)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.STT }, new { @class = "btn btn-warning" }) 
            @Html.ActionLink("Chi tiết", "Details", new { id=item.STT }, new { @class = "btn btn-success" }) 
            @Html.ActionLink("Xóa", "Delete", new { id=item.STT }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
<a href="@Url.Action("Index", "AdminIndex")" class="btn btn-success">Quay lại trang chủ</a>
